# Elevator Controller in Haskell

The implementation lies in `src/Controller.hs`. You do not need to touch any other file. Available control messages are specified in `src/Messages.hs`.

- run with `stack run`. 
- access gui on `localhost:3000/elevator.svg`